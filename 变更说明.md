# G-PID 代码修改说明（Change Notes）

> 适用范围：截至 `Godot-PID-Core` 仓库 `main` 分支（HEAD = `3a2ef32`），含自
> 仓库初始化以来的全部关键改动。
> 文档按「模块」组织，每一节列出 **文件 / 类 / 函数 / 关键改动点**，便于代码评审与回溯。
>
> ⚠️ 当前 `main` 较 `origin/main` 领先 2 个提交（`3a2ef32`、`9f718bf`），尚未推送。
> 需由用户在自有 Terminal 执行：
> `cd Godot-PID-Core && git push`

---

## 0. 提交总览（Commits）

| 提交 | 主题 | 主要内容 |
|------|------|----------|
| `82faac9` | chore: 初始化 Godot-PID-Core 独立工程仓库 | 独立 git 仓库建立 |
| `f163d5b` | feat: 可交互 2D 画布 + 全量显式类型编码规范 | 画布首版、命名前缀、强类型 |
| `4c0c89b` | docs: README 对齐品牌 G-PID + g-pid.com | 品牌统一 |
| `4fd92e3` | docs: README 拆分为中英双文件 + 语言切换链接 | 双语 README |
| `1ac521e` | feat: 按开发指南重构目录结构并建初始模块 + 修复解析错误 | 目录树、桩文件 |
| `dbc47cd` | docs/style: 修复 Toolbar 原生类冲突 + README 重定位 | 安全可信 AI 数据中枢 |
| `91b4ff6` | refactor: 统一 GP/gp 命名 + 注释双语 + README 同步 | 命名/注释规范 |
| `de237c9` | feat: 打包中英文字体 + 界面/图元字号语言设置 + HiDPI | 字体与清晰度 |
| `9f718bf` | feat: 设置/布局/HiDPI 完善 + ISO 标准文件夹名修正 | 布局、跨屏、iso_10628 |
| `3a2ef32` | refactor(canvas): canvas_2d.gd 按 B 版指南重构为 world_root + SymbolView 节点树 | **本次核心重构** |

---

## 1. 仓库与目录结构

**改动要点**

- `Godot-PID/` 作为 WorkBuddy 工作空间（**不入库**）；其下 `Godot-PID-Core/` 为
  独立 git 仓库，即 GitHub `jonsonw/Godot-PID` 的唯一内容。
- 按 B 版开发指南重建目录树（提交 `1ac521e`）：
  - 新增 `docs/{samples,knowledge}`、`assets/symbol_packs/{isa_5_1,iec_62424,iso_10628}`、
    `assets/fonts`、`src/{render,ai,doc,export,save,project,autoload}`、
    `src/addons/symbol_packs`、`scenes/{widgets,symbols}`、`tests`、`weekly`、`.github`。
- `.gitignore` 取消对 `docs/` 的忽略（HTML 仍排除）。
- **拼写修正**（提交 `9f718bf`）：ISO 标准文件夹由 `iso_10648` 改回正确名称 `iso_10628`。

---

## 2. 品牌与 README（双语）

**涉及文件**：`README.md`、`README.en.md`、`CONTRIBUTING.md`

- 品牌定位三层：**产品 G-PID** / **仓库 Godot-PID** / **官网 g-pid.com**。
- README 拆分为 `README.md`（中文默认）+ `README.en.md`（英文），两文件顶部互放语言
  切换链接（提交 `4fd92e3`）。
- 定位由「画图工具」升级为「**贯穿资产全生命周期的数据中枢**」（提交 `dbc47cd`）：
  - 头号卖点：**数据主权 / 无锁定**（单一开放 `*.pid.json`，本地优先、可 Git 版本化）。
  - 第二卖点：**安全可信 AI**（确定性、离线、可审计规则引擎，非云端黑盒 LLM）。
  - 第三卖点：开源 MIT / 自托管。
- 命名节只列事实（品牌 / 仓库 / 官网），去除破折号与括号、不暴露路线图。
- 版权行固定为 `Copyright © 2026 Jonson Wang`（无 LEBTC 等后缀）。
- `CONTRIBUTING.md` 写入 GP/gp 命名规范与显式类型要求。

---

## 3. 编码规范（硬性要求）

**涉及提交**：`f163d5b`、`91b4ff6`

1. **命名前缀**
   - `class_name` = `GP` + PascalCase（如 `GPCanvas2D`、`GPSymbolView`）。
   - 函数 / 方法 / 成员变量 / 信号 / 局部变量 = `gp` + CamelCase（如 `gpAddNode`、
     `gpGraph`、`gpGraphChanged`）。
   - 私有成员 = `_gp` + CamelCase（如 `_gpSyncViews`）。
   - 常量 / 枚举值 = `GP_` + SCREAMING_SNAKE（如 `GP_MODE_SELECT` 风格）；文件名保持 `snake_case`。
   - 禁止改名：Godot 虚方法（`_ready`/`_draw`/`_gui_input`/`_process`）、引擎内置属性
     （`position`/`name`/`size`/`visible`）、字典键（`"id"`/`"from"`/`"to"`）、单字母坐标
     （`x`/`y`/`z`）。
2. **显式类型**（提交 `f163d5b`）
   - 所有变量显式声明类型，禁止 `var x =` / `var x :=` 推断。
   - 容器强类型：`Array[GPSymbolDef]`、`Array[Dictionary]`、`Dictionary` 等。
   - 修复案例：`var label := n["label"]`（`Dictionary` 索引返回 `Variant`，无法推断）
     → 改为 `var label: String` 配合 `if/elif/else`。
3. **双语注释**：英文一行 + 中文一行（类/函数 doc 与行内注释均遵）。
4. **Godot 4 API 修正**
   - `ThemeDB.default_theme`（Godot 3 API，不存在）→ 改为 `ThemeDB.fallback_font`。
   - `class_name` 不得与 Godot 原生类同名（如 `MenuBar` → `GPPIDMenuBar`，提交 `dbc47cd`）。

---

## 4. ⭐ 2D 画布重构（核心，提交 `3a2ef32`）

### 4.1 设计动机（Before → After）

- **重构前**：`canvas_2d.gd` 在 `_draw()` 立即模式里把所有图元/连线一次性绘制，无节点树，
  缩放/平移需手动变换坐标，扩展困难。
- **重构后**（对齐 B 版开发指南）：引入 **`world_root (Node2D)`** 作为世界根，所有图元/连线
  是挂在它下面的真实节点（`GPSymbolView` / `GPEdgeView`）。**平移/缩放只需移动并缩放
  `world_root`**，子节点自动跟随；画布 `_draw()` 只负责背景网格与连接预览，图元本身由各自的
  视图节点绘制。视图是 `GPPIDGraph` 模型的**纯投影**，不持有权威状态。

### 4.2 修改/新增文件清单

| 文件 | 状态 | 类（`class_name extends`） |
|------|------|----------------------------|
| `src/ui/canvas_2d.gd` | **重构** | `GPCanvas2D extends Control` |
| `src/ui/graph_binder.gd` | **新增** | `GPGraphBinder extends Node` |
| `src/render/symbol_view.gd` | **新增** | `GPSymbolView extends Node2D` |
| `src/render/edge_view.gd` | **新增** | `GPEdgeView extends Node2D` |

### 4.3 `GPCanvas2D`（`src/ui/canvas_2d.gd`）改动详情

**新增成员（变量）**

- `gpWorldRoot: Node2D` —— 世界根节点，所有视图节点的父。
- `gpBinder: GPGraphBinder` —— 图绑定器（组合子节点），持有增量视图缓存与同步逻辑。
- `gpViewOffset: Vector2` / `gpViewZoom: float` —— 相机（平移偏移 / 缩放比）。
- 交互状态：`gpMode: int`（`enum GPMode { GP_SELECT, GP_CONNECT }`）、
  `gpPendingDef: GPSymbolDef`、`gpSelectedId: String`、`gpConnectFrom: String`、
  `_gpPanning`、`_gpPanStart`、`_gpPanOffsetStart`、`_gpDragId`、`_gpDragOffset`、
  `_gpLastMouseWorld`。
- 新增信号：`gpStatusUpdated(info: Dictionary)`（底部状态栏快照：选中 id / 缩放 / 世界坐标）。

**新增/重构函数**

| 函数 | 作用 |
|------|------|
| `_ready()` | 创建 `gpWorldRoot` 与 `gpBinder`（`GPGraphBinder`）并 `add_child`；订阅 `I18n.gpLocaleChanged`、`Settings.gpSymbolStyleChanged`；调用 `_gpResetView()` |
| `_gpResetView()` / `_gpApplyCamera()` | 复位视图 / 把 `gpViewOffset`、`gpViewZoom` 应用到 `world_root.position` 与 `scale` |
| `gpScreenFromWorld(w)` / `gpWorldFromScreen(gpS)` | 屏幕↔世界坐标互转 |
| `_draw()` | **只画背景**：先 `_gpSyncViews()`（委托 binder），再画底色、`_gpDrawGrid()`、`_gpDrawConnectPreview()` |
| `_gpSyncViews()` | 委托 `gpBinder.gpSync(gpGraph, gpDefs, gpSelectedId, gpConnectFrom)`（增量同步在 binder 内部完成） |
| `_gpRefreshSymbols()` / `_gpRefreshEdges()` | 委托 `gpBinder.gpRefreshSymbols()` / `gpBinder.gpRefreshEdges()` |
| `_gpNodeCenter(gpId)` / `_gpHitTest(gpWorld)` / `_gpSetNodePos(gpId, gpWorld)` | 中心点 / 命中测试（用 `gpBinder.gpDefFor`）/ 写回节点坐标 |
| `_gui_input(gpEvent)` | 统一输入：**滚轮缩放**、**中键拖拽平移**、**左键**放置/连接/选择+拖拽 |
| `_gpZoomAt(gpScreen, gpFactor)` | 以光标为锚点缩放（zoom 0.25–4.0，步长 12%） |
| `_gpOnLeftDown(gpScreen)` | 左键按下：有待放置图元→新增节点；连接模式→首尾连线；选择模式→选中并可拖拽 |
| `gpZoomStep(gpFactor)` / `gpResetView()` | 公开：以画布中心缩放一步 / 复位居中（供菜单调用） |
| `_gpOnLocaleChanged()` / `_gpOnSymbolStyleChanged()` / `_gpEmitStatus()` | 语言/字体变化刷新；发射状态快照 |
| `_notification(gpWhat)` | 离开场景树时委托 `gpBinder.gpClear()` 清理缓存 |

**保留的兼容接口**：`gpGraph`、`gpDefs`、`gpMode`、`gpPendingDef`、`gpSelectedId`、
`gpConnectFrom`、`gpGraphChanged`、`gpStatusUpdated`、`gpZoomStep`、`gpResetView`。

**关键行为变化**

- 拖拽节点：`_gpSetNodePos` 写回图数据 → 视图 `gpUpdateTransform()` → 边视图 `queue_redraw()`
  （边依赖节点位置）。
- 新增节点时 `label` 留空，绘制时回退到 `I18n.gpTr(gpDef.gpDisplayName)`，随界面语言切换。
- 背景网格随缩放自适应（步长 `50 * zoom`，过密自动跳过）。

### 4.3b `GPGraphBinder`（`src/ui/graph_binder.gd`，新增）

从 `canvas_2d.gd` 抽取的视图-模型绑定器（组合子节点），持有增量视图缓存与同步逻辑。
画布通过组合持有 `gpBinder`，在 `_draw()` 前委托 `gpBinder.gpSync()` 完成视图树与图数据的增量同步。

- **变量**：`gpGraph: GPPIDGraph`、`gpDefs: Array[GPSymbolDef]`、`gpWorldRoot: Node2D`、
  `_gpSymbolViews: Dictionary`（id → GPSymbolView）、`_gpEdgeViews: Dictionary`（id → GPEdgeView）。
- **函数**：
  - `gpSync(gpG, gpD, gpSelectedId, gpConnectFrom)` —— 每帧注入图数据与高亮状态，执行增量同步。
  - `gpDefFor(gpTypeId) -> GPSymbolDef` —— 按 id 查找图元定义（供命中测试用）。
  - `gpGetSymbolView(gpId) -> GPSymbolView` —— 按 id 查找视图节点（供拖拽用）。
  - `gpRefreshSymbols()` / `gpRefreshEdges()` —— 批量 `queue_redraw`。
  - `gpClear()` —— 销毁前清理所有视图节点与缓存。

### 4.4 `GPSymbolView`（`src/render/symbol_view.gd`，新增）

薄视图节点，仅投影一个图元实例。

- **变量**：`gpNodeId`、`gpNode: GPPIDNode`、`gpDef: GPSymbolDef`、
  `gpSelected`、`gpConnectSource`。
- **函数**：
  - `gpInit(gpN, gpD)` —— 绑定图节点与定义，设 `name = "Symbol_<id>"`，同步位置。
  - `gpSetSelected(gpSel)` / `gpSetConnectSource(gpSrc)` —— 切换高亮并重绘。
  - `gpUpdateTransform()` / `_gpUpdateTransform()` —— 从图节点 `pos` 同步 `position`。
  - `_draw()` —— 画矩形填充（按 `gpCategory` 配色）、边框、标签（图元字体）、端口圆点。
  - `_gpCategoryColor(gpCat)` —— 按类目返回颜色（pump/tank/valve/instrument/heat 等）。

### 4.5 `GPEdgeView`（`src/render/edge_view.gd`，新增）

薄视图节点，绘制一条连线。

- **变量**：`gpEdgeId`、`gpEdge: GPEdgeEdge`、`gpGraph: GPPIDGraph`。
- **函数**：
  - `gpInit(gpE, gpG)` —— 绑定边与图，设 `name = "Edge_<id>"`。
  - `_draw()` —— 取 `from`/`to` 两端中心点，绘制连线。
  - `_gpNodeCenter(gpId)` —— 复用图节点坐标查找。

> 两视图节点同处 `world_root` 之下，因此自动随其缩放与平移，无需各自处理相机变换。

---

## 5. 主窗口与布局（`src/ui/main_window.gd`）

**改动要点**（由 `scenes/main.gd` 重命名而来）

- CAD 风格布局（顶菜单 / 左图元库 / 中画布 / 右属性标签页 / 底状态栏）固化为 `main.tscn`，
  脚本只做接线与动态内容注入。
- 停靠栏比例单一真相：`gpLeftRatio = 0.2`、`gpRightRatio = 0.2`（左 1/5、右 1/5、中 3/5）。
- **响应式布局函数**：`_gpApplySplits()`（幂等，由比例推导分隔偏移）、`_gpInitSplits()`
  （等布局完成后初始化）、`_gpOnBodyDragged()` / `_gpOnCenterRightDragged()`（记用户拖拽比例）、
  `_gpOnResized()`（仅宽度变化生效，字号不随窗口缩放）。
- **HiDPI / 多显示器函数**：`_gpApplyDpiScale()`（把 `content_scale_factor` 同步到
  `DisplayServer.screen_get_scale`，根治跨屏发虚）、`_gpOnWindowChanged()`（切屏检测）。
- 状态栏接线：订阅画布 `gpStatusUpdated` → 更新选中/坐标/缩放/状态文案。
- 菜单动作映射：`file_new`/`edit_clear`、`view_zoom_in`/`view_zoom_out`/`view_fit`、
  `edit_delete`、`tool_settings`。
- 本地化：`_gpOnLocaleChanged`、`_gpRefreshStaticText`（Tab 标题、状态键随语言刷新）。

---

## 6. 字体与 HiDPI（清晰度修复）

**涉及文件**：`src/autoload/settings.gd`、`assets/fonts/`

**根因**：Godot 内置默认字体不含 CJK 字形 → 中文大字号回退低质量位图字体发虚。

- **内置字体文件**（拷自 macOS 系统字体，随工程走、跨平台可用）：
  - `assets/fonts/ArialUnicode.ttf`（默认 UI 字体）
  - `assets/fonts/HiraginoSansGB.ttc`（默认图元字体）
- `src/autoload/settings.gd`（`Settings` 单例）新增：
  - `GP_FONT_PRESETS`：字体预设表（键→`{zh, en, "res"|"names"}`），含 `arial_cjk` /
    `hiragino` / `system` / `pingfang` / `helvetica` / `arial` / `menlo`。
  - `gpLoadFont(p_gpKey)` —— 优先 `load(spec["res"])`（FontFile）；编辑器首启未导入时用
    字节兜底构造；headless 下回退 `ThemeDB.fallback_font`（避免分配大字形缓存崩溃）；
    `names` 型走 `SystemFont`。
  - `gpApplyFontSize()`（设根主题 `default_font` + `default_font_size`）、
    `gpApplyLocale()`、`gpApplySymbolStyle()`（构造 `gpSymbolFont` 并发 `gpSymbolStyleChanged`）、
    `gpApply()`、`gpLoad()` / `gpSave()`（持久化到 `user://settings.cfg`）。
  - `gpSymbolFont: Font`、`gpSymbolStyleChanged` 信号供画布逐帧廉价读取。

---

## 7. 核心数据模型（`src/core/`）

| 类（`class_name extends`） | 文件 | 关键成员 / 函数 |
|---------------------------|------|-----------------|
| `GPPIDGraph extends Resource` | `pid_graph.gd` | `gpMeta: Dictionary`、`gpNodes: Array[GPPIDNode]`、`gpEdges: Array[GPPIDEdge]`；`gpNewNode()`/`gpNewEdge()`（工厂）、`gpAddNode(node)`/`gpAddEdge(edge)`、`gpGetNode()`/`gpGetEdge()`/`gpRemoveNode()`/`gpRemoveNodeWithEdges()`、`gpToDict()`/`gpFromDict()`（静态）、信号 `gpNodeAdded` 等 |
| `GPPIDNode extends RefCounted` | `pid_node.gd` | `gpInstanceId`、`gpSymbolId`、`gpTag`、`gpPosition: Vector2`、`gpRotationDeg`、`gpFlipped`、`gpAttrValues`；已补全 `gpToDict()`/`gpFromDict()`（兼容旧字典图形状） |
| `GPPIDEdge extends RefCounted` | `pid_edge.gd` | `gpInstanceId`、`gpFromRef`/`gpToRef: Dictionary`、`gpKind`、`gpRouting: Array[Vector2]`、`gpTag`、`gpAttrs`；已补全 `gpToDict()`/`gpFromDict()`（兼容旧字典图形状） |
| `GPSymbolDef extends Resource` | `symbol_def.gd` | `gpId`、`gpDisplayName`、`gpCategory`、`gpIconPath`、`gpDefaultSize: Vector2`、`gpPorts: Array[Dictionary]`、`gpAttrsSchema`；`gpToDict()`；`enum GPSymbolCategory` |
| `GPSymbolLibrary extends RefCounted` | `symbol_library.gd` | 静态 `gpDefaultDefs()`（5 个内置：pump/tank/valve/instrument/heatex）、`_gpMk()`、`list_by_category()`、`search()` |

> 注意：Step 1.5 对象图重构已完成——`GPPIDGraph` 现在持有 `Array[GPPIDNode]`/`Array[GPPIDEdge]`，
> `canvas_2d.gd`/`main_window.gd`/`inspector.gd` 已改为消费对象；`GPPIDNode`/`GPPIDEdge` 的
> `gpToDict()`/`gpFromDict()` 已补全，且 `gpFromDict` 兼容旧字典图形状（id/type/label/pos/attrs）。

---

## 8. 插件契约（`src/addons/ipid_addon.gd`）

- 由 `src/core/` **迁移**至 `src/addons/`（提交 `91b4ff6` 前后）。
- `GPIPIDAddon extends Resource`：Open-Core 插件契约，外围功能（Pro）通过继承此类挂载，
  核心代码不动。
- 方法：`_gpGetName()`、`_gpGetTools()`、`_gpGetPanels()`、`_gpOnGraphChanged(gpGraph)`、
  `_gpRegisterExporters()`、`_gpRegisterSymbolPacks()`、`_gpOnLoad(gpCtx)`、`_gpOnSync(gpOp)`。

---

## 9. 其它修复

- `Toolbar` 原生类冲突修复（`dbc47cd`）：相关类改名 `GPPIDToolbar` / `GPPIDMenuBar`，
  避免与 Godot 内置类名冲突。
- `ScrollContainer` 警告修复（提交 `de237c9`）。
- 仓库初始化时 `8bae8e7` 的手动 README 编辑导致过 push 被拒，已通过
  `git rebase origin/main` 合并并修正英文拼写（`indurstries` → `industries` 等）。

---

## 10. 后续建议（W2 内核）

1. ✅ 已完成：将 `GPPIDGraph` 由 `Array[Dictionary]` 升级为 `Array[GPPIDNode]` / `Array[GPPIDEdge]`，
   补全 `gpToDict()` / `gpFromDict()`（兼容旧字典图形状）。
2. ✅ 已完成：抽取 `graph_binder.gd`（`GPGraphBinder extends Node`），持有增量视图缓存与同步逻辑，
   画布通过组合持有 binder 并委托 `_gpSyncViews`/`_gpRefreshSymbols`/`_gpRefreshEdges`/`gpDefFor`/`gpGetSymbolView`。
3. 将 `GPSymbolView`/`GPEdgeView` 的 `.uid` 一并纳入提交（当前 `git status` 显示
   `symbol_view.gd.uid` / `edge_view.gd.uid` 未跟踪）。

---

*文档生成时间：2026-08-18 · 对应仓库 HEAD `3a2ef32`*
