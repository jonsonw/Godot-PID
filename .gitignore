# ===== Godot 引擎 =====
.godot/
android/
ios/
# 仅忽略仓库根的导出构建产物目录；务必保留前导斜杠，
# 否则会误伤源码目录 src/export/（导出器脚本，历史事故：三个导出器因此从未入库）。
# Only ignore the export build output dir at the repo root; the leading slash is required,
# otherwise it also matches the source dir src/export/ (exporters were silently untracked).
/export/
*.translation

# ===== OS / 编辑器 =====
.DS_Store
Thumbs.db
*.swp
*~

# ===== WorkBuddy 本地项目数据（含记忆/MCP配置，不入库） =====
.workbuddy/

# ===== 中间产物 / scratch =====
pdf_text.txt
*.tmp
*.log

# ===== 本地开发文档与知识库（仅本地参考，不入库） =====
# 注意：docs/ 现按《开发指南》纳入仓库（ARCHITECTURE.md / samples / knowledge 镜像）。
# 仅排除根目录零散的规划类 HTML（本仓库内不含此类文件，保留规则以防误入）。
*.html
